# 1. Load processed data
# 2. Define X (all numeric features except target) and y = Per_Capita_Sugar_Consumption
# 3. Split train/test (e.g. 80/20 random)
# 4. Train RandomForestRegressor
# 5. Evaluate RMSE & R2, save metrics to outputs/models/metrics.txt
# 6. Save trained model to outputs/models/rf_model.pkl
